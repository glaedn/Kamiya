# Gap And Risk Register

Evidence basis: Kamiya@69adfd2; Cerbanimo@render-deploy ce5eca1.

| ID | Gap or risk | Severity | Likelihood | Evidence | User impact | Technical impact | Proposed mitigation | Dependency | Target packet |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | Project creation can succeed while task generation fails or times out. | High | High | Kamiya@69adfd2: [server/services/cerbanimoClient.ts](../../server/services/cerbanimoClient.ts) :: `generateAndWaitForProjectTasks()`; Cerbanimo: `backend/routes/projects.js` :: separate `/create` and `/auto-generate` | User sees a created project without actionable tasks. | Orphan projects, retries, unclear logs. | Instrument create/generate/activate with request IDs, idempotency keys, and normalized error envelopes. | Existing routes | Packet 002 |
| R-002 | Production project-creation journey errors are not fully proven fixed. | High | Medium | Cerbanimo: `backend/routes/projects.js` :: name length validation; `src/pages/ProjectCreation.jsx` caps title | User may hit unexplained creation failures. | Data constraints can reject LLM-generated fields. | Add backend tests around field length, malformed tasks, no-task generation, and task activation. | Database schema truth | Packet 002 |
| R-003 | Auth0 bridge exists but deployment config is fragile. | High | Medium | Kamiya: [src/lib/authBridge.ts](../../src/lib/authBridge.ts); Cerbanimo: `src/pages/AuthBridge.jsx` uses `VITE_AUTH_BRIDGE_ALLOWED_ORIGINS`; Auth0 callback URLs are external config | Login can complete in Cerbanimo but fail to return to Kamiya. | Token handoff and origin restrictions can drift per environment. | Formalize bridge env checklist, callback validation, and permission introspection endpoint. | Auth0 dashboard access | Packet 004 if Packet 002/003 do not block; otherwise Packet 003 |
| R-004 | Kamiya action queue is partly local and non-durable. | High | High | Kamiya: [shared/types.ts](../../shared/types.ts) :: `actionHistory`; Cerbanimo: `backend/services/ActionQueueService.js` exists but Kamiya create flow bypasses it | User may think actions are audited when only local session records exist. | Audit gaps and divergence from Cerbanimo records. | Route all mutations through Cerbanimo `api_actions` preview/confirm. | API foundation | Packet 005 |
| R-005 | Duplicated orchestration surfaces can conflict. | Medium | High | Cerbanimo: `backend/routes/platform.js`; `backend/routes/api_v1/index.js`; `backend/services/ActionQueueService.js` | Clients may call different APIs for same action. | Duplicate contracts and inconsistent envelopes. | Choose canonical `/api/v1` or document `/platform` compatibility role. | Product decision | Packet 003 |
| R-006 | BullMQ/ioredis dependencies conflict with pg-boss direction. | Medium | High | Cerbanimo: `package.json` includes `bullmq`, `ioredis`; `rg` finds no active imports; `backend/jobs/startWorkers.js` uses pg-boss | Future implementers may pick wrong queue. | Dependency bloat and architecture drift. | Mark as deprecation candidate and remove after verification. | None | Orchestration cleanup packet |
| R-007 | node-cron dependency conflicts with pg-boss scheduling direction. | Low | Medium | Cerbanimo: `package.json` includes `node-cron`; `backend/server.js` says scheduled tasks moved to pg-boss | Confusing operational model. | Extra dependency and stale docs. | Remove or document as reserved. | None | Orchestration cleanup packet |
| R-008 | Backblaze B2 integration failure can block profile/reward uploads. | Medium | Medium | Cerbanimo: `backend/utils/b2.js`; `backend/routes/profile.js`; `backend/routes/rewards.js`; committed `backend/uploads/**` files | Profile picture or badge creation fails. | Temp file cleanup and external storage drift. | Add storage health check, fallback policy, cleanup tests, and remove committed upload artifacts. | B2 credentials | Storage repair packet |
| R-009 | Missing complete OpenAPI and normalized envelopes. | High | High | Cerbanimo: `backend/routes/api_v1/index.js` has skeletal `openApiDocument`; legacy routes return varied shapes | Kamiya cannot reliably parse errors and cards. | Client-specific error handling and duplicate schemas. | Expand OpenAPI, request IDs, envelopes, and response contracts for golden path. | API ownership decision | Packet 003 |
| R-010 | APIs are only partly versioned. | Medium | High | Cerbanimo: `/api/v1` exists, but Kamiya calls legacy `/projects`, `/tasks`, `/kamiya`. | Breaking changes can silently break Kamiya. | Tight coupling to legacy route behavior. | Move Kamiya to `/api/v1` or stable `/platform` with version. | Packet 003 | Packet 003 |
| R-011 | Service and user token strategy is unresolved. | High | Medium | Kamiya sends browser-provided Cerbanimo access token to its API; Cerbanimo also supports `cerb_` API tokens in `apiAuthService.js`. | Login and API calls may work locally but fail in production policy. | Ambiguous trust boundary between frontend, Kamiya API, and Cerbanimo API. | Decide user-delegated token vs service token plus user token exchange. | Auth bridge packet | Packet 004 |
| R-012 | Worker idempotency and retry behavior are insufficient. | High | High | Cerbanimo: `backend/routes/api_v1/index.js` :: `/automation/validation-report` marks idempotency/retry false; `ActionQueueService.confirmAction()` logs queue failure but does not recover automatically | Repeated confirmations or retries can duplicate or lose work. | Audit and automation reliability risk. | Add idempotency keys, retry policy, worker recovery, and stuck-run detector. | Action queue | Packet 005 and Packet 006 |
| R-013 | Backend automated tests do not cover enough route surfaces. | High | High | Cerbanimo has service tests but `backend/package.json` test script exits with "no test specified"; root tests are separate. | Regressions in project/task paths reach users. | Low confidence in routes and DB interactions. | Add targeted integration tests for projects, tasks, chats, actions, and API v1. | Test DB/mocks | Packet 002 |
| R-014 | Prompt-injection exposure through proof links and files is unaddressed. | High | Medium | Cerbanimo: `backend/controllers/taskController.js` stores proof links/reflection; `AutomationWorkerService.runSubmissionValidation()` checks presence only | Malicious proof can influence future AI validation. | AI safety and data handling risk. | Add proof capture isolation, allowlist/sandboxing, and prompt-injection filters before AI review. | Validation pipeline | Packet 008 |
| R-015 | Unauthorized mutation risk in legacy routes. | High | Medium | Cerbanimo: `backend/server.js` ignores errors for some GET wrappers; legacy routes often rely on body `userId` or `auth0_id`; `/api/v1` has scopes but Kamiya uses legacy routes | User could mutate via weakly checked legacy parameters if route assumptions fail. | Security and audit risk. | Move mutations to scoped API v1/action queue and remove body-auth identifiers. | API v1 and auth introspection | Packet 003/004 |
| R-016 | UI surfaces exhibit common AI-generated design patterns and inconsistent polish. | Medium | High | Cerbanimo many CSS/page surfaces; prior visual pass added synthwave/glass styling in Kamiya; no formal design audit exists in docs | Users see inconsistent or unprofessional screens. | Maintenance and conversion risk. | Run UI de-sloppification audit after core flows are stable. | Stable golden path | Packet 011 |
| R-017 | Stale documentation contradicts code. | Medium | High | Kamiya docs describe platform requirements; Cerbanimo README claims implemented features; current code shows partial/scaffold states | Future packets may implement from memory instead of evidence. | Duplicate abstractions and wrong priorities. | Keep this canon updated and require evidence citations in packets. | This canon | Ongoing |
| R-018 | `/platform` and `/api/v1` duplicate capabilities. | Medium | High | Cerbanimo: `backend/routes/platform.js`; `backend/routes/api_v1/index.js` | Client developers are unsure which endpoint set to use. | Contract drift and doubled work. | Decide canonical API surface. | Product-owner decision | Packet 003 |
| R-019 | Render topology runs workers inside API process. | Medium | Medium | Cerbanimo: `render.yaml` has no worker service; `backend/server.js` starts pg-boss in web process | Background work can compete with HTTP traffic on free instance. | Reliability and scaling risk. | Add Render worker service for pg-boss workers. | Worker hardening | Worker deployment packet |
| R-020 | Project-generated task dependency graph is LLM-shaped and can be malformed. | High | Medium | Cerbanimo: `backend/services/taskGenerator.js` prompt asks for dependency IDs; `backend/routes/projects.js` inserts generated dependencies | No active tasks or bad activation if dependencies do not correspond to inserted task IDs. | Data integrity and blocked projects. | Validate dependency graph before insert and repair/reject invalid references. | Task-generation repair | Packet 002 |
| R-021 | Real-stack golden browser tests are gated until Cerbanimo has an isolated e2e database and deterministic bootstrap provider. | High | Medium | Kamiya current branch: [e2e/golden-conversation.integration.spec.ts](../../e2e/golden-conversation.integration.spec.ts) is explicitly skipped without `KAMIYA_REAL_STACK_E2E=1`; Packet 003 requires no production services. | Browser contract can pass while local real Cerbanimo remains unverified. | False confidence if deterministic fixture is mistaken for real-stack proof. | Add Cerbanimo e2e DB guard, deterministic provider flag, and database verifier before marking real-stack green. | Cerbanimo PR #145 descendant | Packet 004 |
| R-022 | Golden Conversation later stages are easy to overpromise. | High | High | [e2e/golden-conversation.feature.md](../../e2e/golden-conversation.feature.md) marks automation classification, execution, validation, rewards, dependency recursion, completion, and follow-on quests as not implemented. | Users may expect full quest-to-completion automation after project creation. | Product/UI may expose controls without authoritative endpoints. | Keep later-stage controls hidden or `fixme` until Cerbanimo exposes tested APIs. | Task automation classification | Packet 004 |
