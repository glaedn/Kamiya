# Implementation Status Ledger

Allowed labels: `WORKING`, `PARTIAL`, `SCAFFOLD`, `MOCKED`, `PROPOSED`, `BLOCKED`, `UNKNOWN`, `DEPRECATED_CANDIDATE`.

Evidence basis: Kamiya@69adfd2; Cerbanimo@render-deploy ce5eca1.

| Capability | Status | Evidence | Current behavior | Missing behavior | Known failure | Recommended next packet |
| --- | --- | --- | --- | --- | --- | --- |
| Kamiya web chat shell | WORKING | Kamiya@69adfd2: [src/App.tsx](../../src/App.tsx) :: `App`, `submitMessage()` | Renders chat UI, login button, saved-chat selector, cards, quick commands, pending action panel. | Accessibility and visual audit not complete. | None proven in current build. | UI de-sloppification audit. |
| Slash command autocomplete | WORKING | Kamiya@69adfd2: [shared/commands.ts](../../shared/commands.ts); [src/components/CommandMenu.tsx](../../src/components/CommandMenu.tsx) | Suggests command list while typing. | No cross-client autocomplete contract. | None proven. | API v1 and cards packet. |
| Kamiya API chat endpoint | WORKING | Kamiya@69adfd2: [server/index.ts](../../server/index.ts) :: `/api/chat/turn`; [server/services/chatService.ts](../../server/services/chatService.ts) :: `handleChatTurn()` | Validates request and returns chat turn response. | Request IDs and normalized envelopes are not present in Kamiya API. | Errors return HTTP 400 for broad cases. | API foundation packet. |
| Intent routing | PARTIAL | Kamiya@69adfd2: [server/services/intentRouter.ts](../../server/services/intentRouter.ts); [server/services/localIntentRouter.ts](../../server/services/localIntentRouter.ts); [server/ai/geminiClient.ts](../../server/ai/geminiClient.ts) | Uses Gemini JSON when configured; local fallback otherwise. | Router is not Cerbanimo-owned and not permission-aware against live schemas. | Model failures silently fall to local behavior. | API v1 function-schema packet. |
| Planning missing-input loop | PARTIAL | Kamiya@69adfd2: [server/services/planningService.ts](../../server/services/planningService.ts); [server/ai/prompts.ts](../../server/ai/prompts.ts) :: `buildPlanningPrompt()` | Infers title, description, outcome, timeline; asks for missing fields or optional deadline. | Not backed by Cerbanimo function schema API. | May still over-ask if model/fallback misreads draft. | Project-creation repair and instrumentation. |
| Action preview in Kamiya | PARTIAL | Kamiya@69adfd2: [server/services/actionPlanner.ts](../../server/services/actionPlanner.ts); [server/services/cardFactory.ts](../../server/services/cardFactory.ts) :: `actionPreviewCard()` | Creates local preview card and waits for confirmation. | Not durable in Cerbanimo unless using platform action APIs. | Refresh/session loss can lose pending actions unless saved chat catches it. | Persistent Action queue and audit log. |
| Local action history in Kamiya | PARTIAL | Kamiya@69adfd2: [shared/types.ts](../../shared/types.ts) :: `ActionExecutionRecord`; [server/services/chatService.ts](../../server/services/chatService.ts) | Stores latest history in session and saved chat. | Durable canonical action/event log in Cerbanimo not used by Kamiya flow. | Can diverge from Cerbanimo action records. | Persistent Action queue and audit log. |
| Cerbanimo project creation route | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/routes/projects.js` :: `router.post('/create')` | Validates required fields, inserts project, creates outcome, attempts activation. | Does not itself invoke task generation; generation is separate. | Production journey previously hit `varchar(100)` on long names; now validation/capping exists but needs tests. | Project-creation-to-task-generation repair. |
| Cerbanimo project task generation | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/routes/projects.js` :: `router.post('/auto-generate')`; `backend/services/taskGenerator.js` :: `autogeneratePlan()`, `autoGenerateTasks()` | Generates plan/tasks synchronously, inserts skills/tasks, activates tasks. | Needs request IDs, idempotency keys, better malformed LLM handling, test coverage. | If Gemini fails or tasks malformed, generation fails or no active tasks appear. | Project-creation-to-task-generation repair. |
| Active task polling from Kamiya | PARTIAL | Kamiya@69adfd2: [server/services/cerbanimoClient.ts](../../server/services/cerbanimoClient.ts) :: `waitForActiveProjectTasks()` | Polls Cerbanimo task-status endpoint for up to 30 seconds. | Should be event-driven or durable status-driven long term. | Timeout produces retry UX. | Project-creation-to-task-generation repair. |
| Task status endpoint | WORKING | Cerbanimo@render-deploy ce5eca1: `backend/routes/projects.js` :: `router.get('/:projectId/task-status')` | Returns all project tasks and active count. | Needs `/api/v1` equivalent and envelope. | Auth behavior for GET routes ignores `jwtCheck` error in wrapper. | API v1 foundation. |
| Task claim/accept | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/routes/tasks.js` :: `router.put('/:taskId/accept')`; `backend/controllers/taskController.js` :: `acceptTask()` | Assigns/reserves active tasks and requires linked identity in route. | Policy and error envelope not normalized. | Discord linking can block acceptance. | Submission-validation foundation. |
| Task submission | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/routes/tasks.js` :: `router.post('/:taskId/submit')`; `backend/controllers/taskController.js` :: `submitTask()` | Requires platform user and proof links, sets submitted state, chooses reviewers. | Proof safety, prompt-injection controls, and validation pipeline incomplete. | Route can fail if skill/user/reviewer data malformed. | Submission-validation pipeline foundation. |
| Task approval and rewards | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/controllers/taskController.js` :: `approveTask()` | Completes tasks when verification not required, awards XP/tokens, notifies, activates dependencies. | Need unified validation/appeal state and automated tests. | Verification-required path returns `pending_verification` without proving final release path. | Submission-validation pipeline foundation. |
| Task activation | WORKING | Cerbanimo@render-deploy ce5eca1: `backend/services/TaskRoutingService.js` :: `activateProjectTasks()` | Activates dependency-free or dependency-completed inactive tasks. | Needs more tests for malformed dependencies and recurring activation triggers. | Notification flood limited, but route-level idempotency not fully proven. | Project-creation repair. |
| Auth0 popup bridge | PARTIAL | Kamiya@69adfd2: [src/lib/authBridge.ts](../../src/lib/authBridge.ts) :: `startCerbanimoLogin()`; Cerbanimo@render-deploy ce5eca1: `src/pages/AuthBridge.jsx` | Popup obtains Auth0 token and posts message back to Kamiya. | Production settings and token strategy need formal contract. | Previously failed on callback URL and allowed origin config. | Auth0 popup bridge and permission introspection. |
| Kamiya saved chats | PARTIAL | Kamiya@69adfd2: [server/services/chatService.ts](../../server/services/chatService.ts) :: `persistChatTurn()`; Cerbanimo@render-deploy ce5eca1: `backend/routes/kamiya_chats.js` | Saves messages/session/name into `users.kamiya_chats`. | No dedicated chat table, search, pagination beyond JSON array. | Requires resolved Cerbanimo user; if token/user mismatch, 401. | Memory/chat persistence hardening. |
| Cerbanimo API v1 envelopes | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/routes/api_v1/index.js`; `backend/utils/apiEnvelope.js` | Provides `/api/v1/openapi.json`, envelopes, request IDs, scoped auth. | OpenAPI paths are skeletal; not all legacy routes are normalized. | Duplicates `/platform` concepts. | API v1 foundation. |
| Cerbanimo `/platform` API | SCAFFOLD | Cerbanimo@render-deploy ce5eca1: `backend/routes/platform.js` | Has permissions, intent-route, planning, actions, automation, memory, render descriptors. | Not clearly canonical versus `/api/v1`; limited planning/routing logic. | Potential duplicate abstraction. | API v1 foundation. |
| Durable action queue | PARTIAL | Cerbanimo@render-deploy ce5eca1: `models/kamiya_api.js` :: `api_actions`; `backend/services/ActionQueueService.js` | Persists previews/events, confirms actions, queues automation runs. | Kamiya golden path does not yet use it for project creation. | Known intents execute direct project/task SQL, bypassing project-generation route. | Persistent Action queue and audit log. |
| Automation worker | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/jobs/workers/automationWorker.js`; `backend/services/AutomationWorkerService.js` | Runs quality checks, validation, reminders, planning, summaries, research stubs. | Idempotency keys, retries per template, dedicated worker service. | API validation report marks idempotency/retry as failing. | `run_quality_checks` worker packet. |
| First worker `run_quality_checks` | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/services/AutomationWorkerService.js` :: `runQualityChecks()`; `backend/services/AutomationWorkerService.test.js` | Checks project/task existence, tasks, description, overdue/blocked state. | Repo target quality checks and external CI integration absent. | Only checks DB state for project target; repository target not implemented. | `run_quality_checks` worker packet. |
| pg-boss worker topology | WORKING | Cerbanimo@render-deploy ce5eca1: `backend/jobs/boss.js`; `backend/jobs/startWorkers.js`; `backend/server.js` :: `boss.start()` | Starts queues and schedules inside API process. | Render worker separation. | API and workers share process resources. | Worker deployment packet. |
| BullMQ/ioredis stack | DEPRECATED_CANDIDATE | Cerbanimo@render-deploy ce5eca1: `package.json` includes `bullmq`, `ioredis`; `rg` found no active backend imports. | Dependency exists without active code path. | Removal or documented exception. | Confusion about orchestration source of truth. | Orchestration cleanup packet. |
| node-cron stack | DEPRECATED_CANDIDATE | Cerbanimo@render-deploy ce5eca1: `package.json`; `backend/server.js` comment says scheduled tasks moved to pg-boss. | Dependency exists without active scheduling. | Removal or documented exception. | Confusion about scheduling source of truth. | Orchestration cleanup packet. |
| Notifications | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/services/NotificationService.js`; `src/hooks/useNotifications.js` | Writes notifications and emits Socket.io. | Notification policy and Kamiya delivery channel not formalized. | Noisy vs encouraging policy unresolved. | Notification policy packet. |
| Discord adapter | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/services/integrations/adapters/DiscordAdapter.js`; `backend/routes/discord_config.js` | Initializes adapter, slash commands, account links, task/need/resource flows. | Kamiya Discord parity not implemented. | Depends on Discord config and linked users. | Multi-platform packet after core APIs. |
| Slack adapter in Kamiya | SCAFFOLD | Kamiya@69adfd2: [server/index.ts](../../server/index.ts) :: `/api/adapters/slack`; [server/services/cardFactory.ts](../../server/services/cardFactory.ts) :: `integrationCard()` | Accepts generic payload and returns channel outbound format. | Signature verification and Slack app integration. | `KAMIYA_REQUIRE_CHANNEL_SIGNATURES` exists in Render env but not enforced in endpoint. | Deferred per first-cycle exclusions. |
| Google Chat adapter | SCAFFOLD | Kamiya@69adfd2: [server/index.ts](../../server/index.ts) :: `/api/adapters/google-chat`; Cerbanimo: `GoogleChatAdapter.js` registered | Generic endpoint and adapter registration exist. | Real install/config/user mapping not proven. | Unknown production readiness. | Multi-platform packet. |
| Backblaze B2 uploads | PARTIAL | Cerbanimo@render-deploy ce5eca1: `backend/utils/b2.js`; `backend/routes/profile.js`; `backend/routes/rewards.js` | Uploads files and generates signed URLs. | Robust failure UX and cleanup policy. | User-reported B2 failures remain a risk; committed upload files contradict external storage. | Storage repair packet. |
| GitHub integration | PROPOSED | Cerbanimo@render-deploy ce5eca1: `backend/services/CapabilityRegistryService.js` templates; `AutomationWorkerService.externalIntegrationBlocked()` | Templates exist but worker blocks without credentials/config. | Actual issue/PR/review implementation. | External integration blocked. | GitHub integration packet. |
| Public SDK | SCAFFOLD | Kamiya@69adfd2: [shared/sdk.ts](../../shared/sdk.ts); [package.json](../../package.json) exports | Shared TypeScript export exists. | Published package, stable API, generated client. | None proven. | API v1 foundation. |
| Render deployment | PARTIAL | Kamiya@69adfd2: [render.yaml](../../render.yaml); Cerbanimo@render-deploy ce5eca1: `render.yaml` | Kamiya web service; Cerbanimo API web service and static frontend. | Cerbanimo worker service; full env var parity. | Cerbanimo `server.js` requires vars not all defined by blueprint. | Render deployment hardening. |
